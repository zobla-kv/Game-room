const PORT = process.env.PORT;const io=require("./webSocket"),path=require("path"),express=require("express"),app=express(),server=app.listen(PORT || 4000);io.listen(server),app.use(express.static(path.join(__dirname,"..","/public")));
